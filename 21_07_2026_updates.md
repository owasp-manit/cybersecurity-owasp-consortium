feat(ui): upgrade static wallpaper, favicons, event cards, and responsive layout

- **Static Wallpaper System**: Implemented full-screen static network constellation canvas (`#static-network-canvas`) and enlarged 80px clean multi-color grid matrix (`.ambient--static`) for secondary content pages with 0% CPU scrolling overhead.
- **Branding & Favicons**: Added official OWASP logo favicons across all HTML pages (`index.html`, `SITE/*.html`, `events/*.html`, `OLD/*.html`) and animated boot logo HUD screen.
- **Event Cards & Fallback**: Created dedicated `images/image_load_error.svg` placeholder asset and `onerror` handling for `.event-card__media` when images are missing or fail to load.
- **Responsive Components**: Upgraded `.events-overview` stat cards with center-aligned values (`text-align: center !important`) and fluid 4-col/2-col/1-col responsive breakpoints.
- **Modular JavaScript**: Extracted dynamic particle background into `SITE/Wallpaper_engine.js` and refactored `SITE/script.js` into clean UI modules.
